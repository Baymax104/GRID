# @package _global_

defaults:
- /data@data: rkmeans_inference
- /model@model: rkmeans_inference
- /callbacks@callbacks: rkmeans_inference
- /logger@logger: rkmeans_inference
- /trainer@trainer: rkmeans_inference

data_dir: ???
embedding_path: ???
codebook_size: ???
num_hierarchies: ???
embedding_dim: ???
ckpt_path: ???
devices: ???

run_mode: inference
task_name: rkmeans_inference
id: ${now_tz:%Y-%m-%d}/${now_tz:%H-%M-%S}
seed: 42
paths:
  root_dir: .
  data_dir: ${data_dir}
  log_dir: ${paths.root_dir}/logs
  output_dir: ${hydra:runtime.output_dir}
  work_dir: ${hydra:runtime.cwd}
  profile_dir: ${hydra:run.dir}/profile_output
  metadata_dir: ${paths.output_dir}/metadata
extras:
  ignore_warnings: false
  print_config_warnings: true
  print_config: true
